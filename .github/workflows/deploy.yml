name: Deploy to Hostinger

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: royalblue-spoonbill-538881.hostingersite.com
          port: 21
          username: u428207087.romanza.site
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /public_html/
          security: ftpes
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            project/**
            .github/**
